'use client'

import InstallPrompt from '../ui/InstallPrompt'

/**
 * 대시보드용 설치 프롬프트 래퍼
 * 서버 컴포넌트에서 클라이언트 컴포넌트를 사용하기 위한 래퍼
 */
export default function DashboardInstallPrompt() {
  return <InstallPrompt />
}

